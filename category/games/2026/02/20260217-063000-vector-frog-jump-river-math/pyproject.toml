[project]
name = "vector-frog-jump-river-math"
version = "0.1.0"
description = "Cross the river by jumping on lily pads with correct mathematical answers."
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.1",
]
