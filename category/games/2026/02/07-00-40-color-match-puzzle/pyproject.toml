[project]
name = "color-match-puzzle"
version = "0.1.0"
description = "Addictive classic-style puzzle game connecting same-colored blocks"
requires-python = ">=3.12"

[project.scripts]
color-match-puzzle = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
